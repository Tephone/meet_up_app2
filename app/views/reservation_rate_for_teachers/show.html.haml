%h4 #{@today.month}月の講師毎の予約率

%table
  - @DAY.each do |day|
    %th= day
  - @calendar_date_for_week.each do |week|
    %tr
      - week.each do |date|
        - day_of_week = @from.wday
        - if date == @from #もし月初の１日目だった場合
          -# 日付けを右詰めにするための処理day_of_weekの数分、空の%tdをいれる、ex)火曜だと2なので2こ空の%td入れる
          - day_of_week.times do
            %td 
        %td
          %p= date.day
          %p --%
